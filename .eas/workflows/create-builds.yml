name: Create builds

on:
  push:
    branches: ['master']

jobs:
  build_android:
    name: Build Android app
    type: build
    params:
      platform: android
      profile: development
  build_ios:
    name: Build iOS app
    type: build
    params:
      platform: ios
      profile: development
